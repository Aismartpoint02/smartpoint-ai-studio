# SmartPoint AI Studio V29 — MAX LAUNCH
Additive upgrade of V28.5. No existing workspace was intentionally removed.

Run: `npm install` → `npm run typecheck` → `npm run build` → `npm run dev`.

Production secrets stay server-side. Configure Cloudflare AI, Google OAuth and Paystack only through your deployment environment.
